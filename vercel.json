{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node",
            "config": { "maxLambdaSize": "10mb", "node": "20.x" }
        }
    ],
    "routes": [
        { "src": "/.*", "dest": "/index.html" }
    ]
}
