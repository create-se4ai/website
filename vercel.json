{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": { "maxLambdaSize": "10mb" },
        "buildCommand":"yarn build" 
      }
    ],
    "routes": [
      { "src": "/.*", "dest": "/index.html" }
    ]
  }
  